"""Pipeline do agente — placeholder da Fase 1.

Na Fase 2 (ver `FUNCIONALIDADES.md` · F2) entram aqui os componentes isolados:
    ingest → redact (PII) → prompts → extract (Claude) → score (Python) → service

Princípio: a IA só extrai sinais; o Python aplica a rubrica e calcula a nota.
"""
