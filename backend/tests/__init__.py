# Pacote de testes (regular) — garante que `tests` resolva para este diretório,
# não para um pacote `tests` de terceiros instalado no ambiente (ex.: ambientes
# anaconda compartilhados com outras libs).
